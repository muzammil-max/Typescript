//Constants
const animal = "Lion";
//when uncommenting this code : const animal = "Rat"; will give error this is because in constant the value cannot be changed as it has fixed value
console.log (animal);